export const metadata = {
  title: "Create Account",
  description: "Join CreatorOS AI and start generating content today.",
};

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
